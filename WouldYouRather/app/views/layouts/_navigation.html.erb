<nav class="navbar navbar-inverse">
  <div class="container-fluid">
    <!-- Brand and toggle get grouped for better mobile display -->
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1" aria-expanded="false">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <strong><%= link_to 'WouldYouRather',root_path,class: "navbar-brand",id: "wyr" %></strong>
    </div>

    <!-- Collect the nav links, forms, and other content for toggling -->
    <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
      <ul class="nav navbar-nav">
        <li ><%= link_to 'Index',main_index_path %></li>
        <li><a href="#">Link</a></li>
        <li class="dropdown">
          <a href=" " class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false"><span class="glyphicon glyphicon-th-list"></span> Categorias <span class="caret"></span></a>
          <ul class="dropdown-menu">
            <% categories.each do |cat| %>
                <li><%= link_to '<i class="glyphicon glyphicon-tag"></i>'.html_safe+' '+cat.name.capitalize,questions_findbycategory_path(categoryname: cat.name) %></li>
            <% end %>
            <li role="separator" class="divider"></li>
            <li><%= link_to 'Todas',questions_path %></li>
          </ul>
        </li>
      </ul>
      <form class="navbar-form navbar-left">
        <div class="form-group">
          <input type="text" class="form-control" placeholder="Procurar pergunta">
        </div>
        <button type="submit" class="btn btn-default"><span class="glyphicon glyphicon-search"></span></button>
      </form>
      <ul class="nav navbar-nav navbar-right">
        <% if user_signed_in? %> 
          <li><%= link_to '<i class="glyphicon glyphicon-log-out"></i> Logout'.html_safe,destroy_user_session_path,method: :delete %></li>
        <% else %>  
          <li><%= link_to '<i class="glyphicon glyphicon-log-in"></i> Login'.html_safe,new_user_session_path %></li>
          <li><%= link_to '<i class="glyphicon glyphicon-user"></i> Cadastro'.html_safe,new_user_registration_path %>
        <% end %>
        </li>
      </ul>
    </div><!-- /.navbar-collapse -->
  </div><!-- /.container-fluid -->
</nav>